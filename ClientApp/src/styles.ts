// Centralized CSS imports so other modules can import a single entry.
import './index.css';
import './App.css';

// Export a sentinel so consumers can import something if needed.
export const stylesLoaded = true;
